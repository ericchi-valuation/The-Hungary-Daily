# 🏆 技術總結：Taiwan Daily Insider 全自動 AI 廣播生態系

恭喜您！您的 `Taiwan Daily Insider` 專案已經成功進化為一個 100% 全自動、無須人力介入的 AI 媒體帝國。以下是我們建立的完整系統架構總結，供您未來維護或擴充時參考。

## ⚙️ 系統工作流程 (Workflow)

整個系統由 GitHub Actions (`.github/workflows/daily_podcast.yml`) 負責每日定時驅動，執行順序如下：

1.  **資料採集 (`fetchers/`):** 
    - `news_fetcher.py`: 爬取最新的台灣政經新聞。
    - `social_fetcher.py`: 爬取 PTT/Dcard 等社群熱門話題。
2.  **腳本生成 (`core/script_generator.py`):** 
    - 透過 Gemini API，將冷硬的新聞資料轉換成流暢、美式 Podcast 風格的口語英文廣播稿。
3.  **音訊合成與混音 (`core/audio_builder.py` & `audio_mixer.py`):** 
    - 將文稿轉為擬真人聲音檔 (現階段使用 edge-tts/ElevenLabs)。
    - 結合 `bgm.mp3` 背景音樂，混音產出最終的 `TaiwanDaily_Podcast_Final.mp3`。
4.  **內容分發 (Content Distribution):**
    - **RSS 發布 (`core/rss_generator.py`):** 產生包含完整 Author 與 Email 標籤的 `feed.xml`，用於自動同步更新至 Apple Podcasts 與 Spotify。
    - **電子報 (`publishers/email_sender.py`):** 透過 Gemini 將廣播稿改寫成 HTML 排版，並使用 Gmail SMTP 自動寄信給 `subscribers.txt` 內的訂閱者。
    - **社群貼文 (`publishers/threads_poster.py`):** 透過 Gemini 萃取 500 字以內的精華，結合 Meta Graph API，自動發布至 Threads。

## 🔐 核心安全機制 (Secrets)

整個系統依賴 5 支存放於 GitHub Secrets 的金鑰來確保運作安全，代碼中沒有寫死任何機密：
- `GEMINI_API_KEY`: Google AI 引擎大腦
- `GMAIL_ADDRESS` & `GMAIL_APP_PASSWORD`: 用於寄送 Email
- `THREADS_USER_ID` & `THREADS_ACCESS_TOKEN`: 用於發布 Meta Threads

> [!NOTE]
> **維護建議**
> 如果未來需要新增電子報訂閱者，只要在 GitHub 編輯 `subscribers.txt` 新增 Email 並存檔即可，下一次發布就會自動生效。
